


const students = ['João', 'Maria', 'Vitor']
/* Para adicionar itens na lista utilizamos o .push. */
students.push('Renan')
/* Também posso adicionar diretamente a posição como no exemplo abaixo.*/
students[4] = 'Bryan'
console.log(students);

/* Também temo os .pop que retira a ultima posiçao mais proxima.
    Temos o .shift que retira a primeira posição. 
    .lenght devolve o tamanho da lista.
    */